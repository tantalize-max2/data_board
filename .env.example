# 数据库配置
DB_HOST=mysql
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_secure_password_here
DB_DATABASE=xiashou2

# 连接池（150并发建议 min=4 max=20 max_conn=50）
DB_POOL_MIN=4
DB_POOL_MAX=20
DB_POOL_MAX_CONN=50

# Uvicorn worker 数（一般等于CPU核心数）
UVICORN_WORKERS=4
